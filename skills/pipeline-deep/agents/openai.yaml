interface:
  display_name: "Pipeline Deep"
  short_description: "Discovery pipeline; heavy means C4 A3"
  default_prompt: "Use $pipeline-deep for this uncertain task. If the user says heavy, режим heavy, autonom+heavy, autonomous heavy, or ok, normalize run controls to C4 A3. Treat P as forcing premortem plan review, not as another mode. Treat -P as skipping only optional premortem; do not bypass mandatory high-risk safety gates."
